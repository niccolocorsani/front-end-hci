import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-project-cordova',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
